<Window x:Class="Decompiller.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Decompiller"
        xmlns:avalonEdit="http://icsharpcode.net/sharpdevelop/avalonedit"
        mc:Ignorable="d"
        Background="#2D2D30"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.Resources>
            <Style TargetType="avalonEdit:TextEditor">
                <Setter Property="Margin" Value="5"/>
                <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
                <Setter Property="HorizontalScrollBarVisibility" Value="Auto"/>
                <Setter Property="Background" Value="#1E1E1E"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="FontWeight" Value="Bold"/>
                <Setter Property="FontSize" Value="14"/>
            </Style>
            <Style TargetType="StackPanel">
                <Setter Property="Margin" Value="5, 5, 5, 0"/>
                <Setter Property="Orientation" Value="Horizontal" />
            </Style>
            <Style TargetType="Button">
                <Setter Property="Margin" Value="5"/>
                <Setter Property="Height" Value="30" />
                <Setter Property="Width" Value="80" />
            </Style>
        </Grid.Resources>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel>
            <Button>Open</Button>
            <Button>Compile</Button>
        </StackPanel>

        <avalonEdit:TextEditor Grid.Row="1" x:Name="IlEditor" ShowLineNumbers="True" SyntaxHighlighting="C#" />

        <avalonEdit:TextEditor Grid.Column="1" Grid.Row="1" x:Name="SharpEditor" ShowLineNumbers="True" SyntaxHighlighting="C#" />

    </Grid>
</Window>
